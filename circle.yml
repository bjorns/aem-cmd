dependencies:
    pre:
        - pip install -r requirements.txt

test:
    override:
        - nosetests --with-coverage --cover-package=acmd --cover-min-percentage=75 --cover-xml --cover-xml-file=build/coverage-data.xml
    post:
        - python setup.py bdist_wheel
